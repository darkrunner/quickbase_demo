dependencies {
    implementation 'org.postgresql:postgresql'
}

bootJar {
    baseName = 'demo'
    version = project.version
}

docker {
    name "${project.name}:${project.version}"
    files bootJar
}